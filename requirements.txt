# Core data + AWS
pandas>=2.0,<3.0
boto3>=1.26,<2.0
requests>=2.31,<3.0
python-dotenv>=1.0

# OpenAI (NEW SDK – required for LangChain)
openai>=1.10,<2.0

# LangChain (minimal, stable wedge)
langchain>=0.1.17,<0.3.0
langchain-core>=0.1.48,<0.3.0
langchain-openai>=0.1.7,<0.3.0

# Validation / schemas
pydantic>=2.5,<3.0

# Storage / filesystem
fsspec>=2024.6.0,<2025.0.0
s3fs>=2024.6.0,<2025.0.0

# ML (RAG / TF-IDF)
scikit-learn>=1.3,<2.0

# Embeddings RAG (Transformers)
transformers>=4.40,<5.0
sentence-transformers>=2.7,<4.0

# Testing
pytest>=8.0,<9.0
